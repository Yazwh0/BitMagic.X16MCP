import BM="bm.bmasm";

BM.X16Header("entry");

.constvar byte counter $02
.proc entry
    sei

    jsr counting_test

    brk
.endproc

.proc counting_test
    lda #0
    sta counter

.loop:
    inc counter
    lda counter
    cmp #10
    bne loop

    rts
.endproc
